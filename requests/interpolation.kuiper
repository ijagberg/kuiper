{
  "uri": "http://localhost/{{env:route}}",
  "method": "GET",
  "params": {
    "env_1": "{{env:query_param_1}}",
    "expr_uuid": "{{expr:uuid}}",
    "expr_now": "{{expr:now}}"
  },
  "headers": {
    "interpolated_header": "{{env:header_value}}"
  }
}
